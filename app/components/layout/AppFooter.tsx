"use client";

export function AppFooter() {
  return (
    <footer className="border-t border-zinc-800 px-4 sm:px-8 py-4 text-zinc-700 text-xs text-center font-mono">
      Primitive triples via Gaussian integers · Integer-area triangles via
      Heron's formula
    </footer>
  );
}
